# Port the proxy listens on (default: 3000)
PORT=3000

# Timeout in ms for requests to the target (default: 30000)
REQUEST_TIMEOUT_MS=30000

# Redact the Authorization header value in logs (default: true)
# Set to false only when debugging auth issues
REDACT_AUTH=true
